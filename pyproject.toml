[project]
name = "pdf-to-images"
version = "0.1.0"
description = "Convert all PDFs in input/ to per-page PNG images in output/"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pdf2image>=1.17.0",
]

[project.scripts]
pdf-to-images = "main:main"
